#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs- the function for getting the absolute
 *@c: the number to get the absolute.
 * Return: Absolute value.
 */

int _abs(int c)
{
	int x;

	x = abs(c)
	if (x)
	{
		return (c);
}
