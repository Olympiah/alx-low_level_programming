#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @x: first int number to be added.
 * @y: secondint number to be added.
 *
 * Return: the value of addition of x and y.
 */

int add(int x, int y)
{
	int result;
	result = x + y;
	return (result);
}
